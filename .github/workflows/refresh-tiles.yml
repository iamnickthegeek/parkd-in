name: Refresh Tiles

on:
  schedule:
    - cron: "0 6 * * *"   # Every day at 06:00 UTC
  workflow_dispatch:        # Manual trigger from GitHub

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install mapbox-vector-tile requests

      - name: Generate tiles and segments.json
        env:
          MAPBOX_TOKEN: ${{ secrets.MAPBOX_TOKEN }}
        run: python generate_tiles_local.py

      - name: Commit and push updated tiles + segments
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/tiles/ public/segments.json
          git diff --cached --quiet || git commit -m "chore: refresh parking tiles [skip ci]"
          git push
